# A Simple React Template

### If your `git` `nodejs` `pnpm` is installed
```bash
# start

git clone https://github.com/pingjintao/demo_react.git dirName

cd dirName

pnpm i

pnpm dev

# build

pnpm build
```